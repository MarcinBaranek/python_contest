# coding=utf-8


def fib(n) -> int:
    """returns the n-th element of the Fibonacci sequence.

    >>> fib(0)
    1
    >>> fib(1)
    1
    >>> fib(5) == fib(4) + fib(3)
    True
    """
    return 0
